{
    "scripts": {
        "preupdate": "echo 'Bumping version' && npm run test && npm run build",
        "postcommit": "git push && git push --tags && npm publish --access public && npm run docs && npm run ghpages",
        "postupdate": "echo 'Updated to version %s in manifest files'"
    }
}
