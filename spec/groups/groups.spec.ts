// import {cli } from "../../src";

describe("groups", () => {
    // let groupsFixture = require('./fixture');
    // cli.config({ autoExecute: false, mode: 'groups' })
    describe('Check for awesome', () => {
        it('should be awesome', () => expect(true).toBeTruthy())

    });
});

